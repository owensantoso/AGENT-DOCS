# v3 change log

Compared with the original seed and audit addendum, v3:

- implements the corrected normalizer;
- implements a working matcher rather than only scoring suggestions;
- passes every original and adversarial case;
- changes the audit’s direct-no Copium restriction according to Owen’s explicit decision;
- adds an always-visible Copium disclaimer;
- adds the four missing concepts;
- migrates generic changed-feelings language away from `someone_else`;
- downgrades broad context-dependent aliases;
- introduces stable records rather than arrays of anonymous strings;
- splits concepts into separate files;
- generates stable share routes, HTML pages, and PNG cards;
- adds privacy redaction, escaping, CSP, and lookup hardening;
- produces reproducible build and validation reports.
