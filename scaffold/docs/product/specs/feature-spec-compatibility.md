# Feature Spec Template

Compatibility wrapper for older workflows.

Create new specs with `agent-continuity docs new spec "<title>" --spec-type feature`. Fresh records use UUIDv7 identity, an empty alias list, and a `SPEC-<slug>.md` locator.
