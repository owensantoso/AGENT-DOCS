#!/usr/bin/env bash
set -euo pipefail

# Legacy compatibility entry point. New projects should use:
#   agent-continuity docs ...
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/agent-continuity-docs" "$@"
