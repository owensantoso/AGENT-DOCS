#!/usr/bin/env bash
set -euo pipefail

# Legacy compatibility entry point. New usage is `agent-continuity`.
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export AGENT_CONTINUITY_COMMAND_NAME="agent-docs"
exec "$script_dir/agent-continuity" "$@"
