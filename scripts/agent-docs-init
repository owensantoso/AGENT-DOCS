#!/usr/bin/env bash
set -euo pipefail

# Legacy compatibility entry point. New usage is `agent-continuity init`.
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export AGENT_CONTINUITY_INIT_PROG="agent-docs-init"
exec "$script_dir/agent-continuity-init" "$@"
